import React from 'react';
import { useTitle } from '../../hooks/useTitle';

const Blogs = () => {
    useTitle('Blogs')
    return (
        <div>
            Blog Page
        </div>
    );
};

export default Blogs;